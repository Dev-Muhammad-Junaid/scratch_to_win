name: scratch_to_win
description: Scratch-off overlay for Flutter — reveal widgets with gestures, progress, confetti, and haptics.
version: 0.3.0
homepage: https://github.com/Dev-Muhammad-Junaid/scratch_to_win
repository: https://github.com/Dev-Muhammad-Junaid/scratch_to_win
issue_tracker: https://github.com/Dev-Muhammad-Junaid/scratch_to_win/issues

topics:
  - ui
  - animation
  - widget
  - game

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
